#include "main.h"

/**
 * main - Entry point of the program.
 *
 * Return: always 0 (success).
 */
int main(void)
{
	int quit = 0;

	while (quit == 0)
	{
		print_game_title();
		quit = 1;
	}

	return (0);
}
